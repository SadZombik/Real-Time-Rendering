#ifndef TESTS_H
#define TESTS_H

#include <glm/glm.hpp>

struct GLFWwindow;

namespace BasicTransforms {
    int Init();
    void TestTranslation();
    void Clear();

}; // namespace BasicTransforms

#endif // TESTS_H